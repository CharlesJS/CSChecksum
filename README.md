# CSChecksum

A Swifty library for easily calculating a variety of checksum and cryptographic hash types.
Does not require Foundation, but does include an optional `Foundation` trait which, when enabled, will allow the use of the `Data` type.
